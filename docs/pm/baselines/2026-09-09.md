# 2026-09-09 · 重构前基线

用户已授权 Git 提交和远程推送。保存到 `baseline/personal-archive-20260909`，作为规划和后续开发的可追溯起点；不是视觉验收通过或生产发布声明。起始提交 `55c4ad384f15dea3147db5832f8810d7f34ccf47`。

远程产品基线提交：`6137099b950745d06c1f2894d83cdf7a1826684a`；已通过 `git ls-remote` 核对。该提交后只读规划的报告另行记录，不改变此产品基线。

## 保存范围

当前自然光房间、常驻空间、实际阅读交接/返回、统一纸色、资源/像素处理、网页 GLB 与场景契约、制作工具、历史交付记录和 PM 规划文档。未开发叙事内核迁移方案。依 tim 最新指示，将 chunk 体积阈值改为测量提示；缺失资源与加载边界检查仍保留。

[文件指纹](2026-09-09-files.json) 记录本轮变更的产品/工具文件及资产。原始 Blender、贴图、渲染和历史备份依 `art/personal-archive/.gitignore` 保留为本地文件；JSON 中 Blender 的 local_only 条目只有指纹，远端没有完整源工程备份。网页 GLB 内包含网页纹理。

## 本轮实际检查

环境：本地 macOS，Node v25.8.1；范围为 Landing 与网页模型。命令在仓库根目录执行，模型检查也依赖本地忽略的贴图。

| 检查 | 结果 / 退出码 |
| --- | --- |
| `npm run build:landing`（含 TypeScript 构建） | PASS / 0；有 chunk 大小及 dynamic import 警告 |
| `npm run lint:landing` | PASS / 0 |
| `npm run test:unit --workspace @timcai/landing` | 113/113 PASS / 0 |
| `node tools/personal_space/checks/verify-model.mjs` | PASS / 0；GLB 21.83 MiB，动画与贴图/场景契约通过 |
| 首轮 `npm run test:guards --workspace @timcai/landing` | FAIL / 1；JS gzip 584.4 KB 超过原 584 KB 上限，在第一项停止 |
| 按用户决定修改后 `npm run test:build:chunks --workspace @timcai/landing` | PASS / 0；JS 584.4 KB、CSS 152.4 KB，仅提示体积，缺失资源检查仍阻断 |
| 最终 `npm run test:guards --workspace @timcai/landing` | FAIL / 1；前七项通过，仅最后 vendor integrity 失败 |
| 单独执行其余 chapter-state/frame/loader/content/bytes/effects 六项守卫 | 全部 PASS / 0 |
| 单独执行 canvas-vendor-integrity | FAIL / 1；DecryptRevealVanilla.ts 指纹与登记值不符 |
| `git diff --check` / 暂存区检查 | 初次暂存区检查发现文档硬换行双空格；改为等价 `<br>` 后最终暂存区检查 PASS / 0 |

vendor expected：`97b9d215d98bdc7cc49f953ab686c8f6c4097cbba173f1514113dabae54098be`；actual：`f59883071a44403f667f55eef8089628e9a7de28e79b9988de63561220944b1d`。已实际比对该 vendor 文件与起始 HEAD，字节指纹一致；本轮未改该文件或其登记值。vendor integrity 为仍存在的基线问题。体积依据用户明确要求转为提示，不作为失败或重构前置条件。

原始日志在本地 `output/pm/baseline-2026-09-09/`，被 Git 忽略。此表保留远端可读的实际结果。没有重跑浏览器 E2E、Studio、线上部署检查或美术验收，不继承历史通过结论。

## 下一步

ARCH 接 NR-00，只读核对和完善迁移计划；PM 审查后让 tim 确认，再派开发卡。DEV/QA 初始化不等于开发或验收任务已开工。
