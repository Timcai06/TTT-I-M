# NR-00 · 基线核对与最小迁移合同

- 版本：1。类型：规划卡，只读产品；状态由看板维护。
- 负责会话：ARCH，`01a08483-8c0c-7fd0-b1a5-9ecc926e4f30`。
- 模型：`gpt-6-astra` / `high`。优先级：P1；依赖：基线提交推送完成。
- 目标：将用户的架构方案收敛为能审查、能逐卡开发的具体迁移合同；本卡不批准任何实现。

## 输入

仓库 `/Users/tim/DEV/TTT I'M/portfolio`。阅读 PM 入口、`narrative-runtime-plan.md`、`baselines/2026-09-09.md` 与用户方案；再按问题读取实际相关代码。基线分支 `baseline/personal-archive-20260909`，执行时记录实际完整 HEAD 并核对产品文件指纹，不能继续使用旧远端 55c4ad3 代表当前实现。

已知 vendor integrity 未通过，具体证据见基线记录。tim 已明确体积不作为硬性要求，体积检查改为提示。先分类其与迁移的关系，不在本卡修复或自行新增硬门槛。

## 权限与协作

- 可读：仓库相关代码、资产合同、必要 GLB 结构与已有证据。只读取资产，不运行 Blender/导出器。
- 独占写入：`docs/pm/reports/NR-00-architecture.md`、`docs/pm/reports/NR-00-contract.md`；需要长清单时写 `output/pm/NR-00/`，不得覆盖已有文件。
- 不可写：产品代码、测试、资产、PM 简报/看板/任务卡、其他报告。
- 不启动服务、不构建、不测试、不提交/推送、不创建 worktree/会话，不给 DEV 或 QA 派任务。你不是唯一执行者，保留他人全部变更。
- 如发现产品版本漂移，记录变化并向 PM 报告；不得把两个版本的状态混成一份确定结论。

## 交付内容

1. **现状对照**：逐条区分附件仍适用、已实现、已过期和未知部分，给文件/符号依据。
2. **受控状态清单**：样段相关物体、可见性、动画权重/时间、相机、DOM、会话记忆分别由谁写；哪些可由 T 推导，哪些属于显式 U/短期输入/事件。先覆盖完整写入域，不扩成全仓框架。
3. **最小接口合同**：StoryPosition、布局版本、采样输入/输出、绑定、最终相机/投影快照、取消与提交语义；仅必要的类型草案和调用关系，不写生产实现。
4. **样段对象表**：About → Life → Frame 的源/目标物件、真实图片 ID/裁切、交互所有权及边界姿态。不要把不同照片自动合成同一 ID；内容取舍列为 PM/tim 决策。
5. **NR-01/02/03 拆分建议**：逐文件所有权、依赖、影子采样接管位置、旧路径退出与回退、各卡可感知结果；说明最小同帧执行为何不会与旧路径竞争。
6. **验收建议**：纯状态与真实场景两层检查，段落边界/反向/seek/布局改变/恢复；说明如何取真实读回证据，容差须有理由，未测则不称 PASS。
7. **待决策项**：最多 5 项关键决定，每项给明确推荐、理由与代价；避免把普通实现选择全部上交用户。

## 验收项

- AC-1：基线实际可识别，附件过期假设得到校正，没有虚构检查结果。
- AC-2：样段受控属性与新旧所有权足够明确，PM 能识别竞争写入和回退风险。
- AC-3：NR-01/02/03 能据此转成单写者、可检查、可回退的实施卡；尚待产品判断的项单列。
- AC-4：产品零修改；两个报告路径可读，按报告模板给出结构化摘要。

`NR-00-contract.md` 为详细合同，建议以表格和必要伪代码表达；`NR-00-architecture.md` 按报告模板给简洁回报并链接合同。完成后结束，等待 PM 审查。用户确认计划后才会另行派实施卡。
