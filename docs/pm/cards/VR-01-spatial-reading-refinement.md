# VR-01 · 空间滚动、阅读衔接与前景配色

PM_ACCEPTED_IMPLEMENTATION · 2026-09-10 · 已完成并停止。24文件接受清单output/pm/VR-01/pm-accepted-files.json，22完整文本及2二进制快照已保存。PM独立真实几何反例修复后复验通过；Work内层主题最后修正已纳入。最终集成QA/用户前端验收另记。下方为本卡执行记录，不能据此自行续写。

## 授权与结果

tim已确认本轮理解并授权“做完全部修改”，前端检验与效果测试由tim完成。本轮完成实现和必要非浏览器技术验证，不启动浏览器验收、不替用户宣称视觉通过。最终收尾、提交、推送、发布未授权。保护5173与全部已有未提交成果。

1. 3D视角切换的滚动手感与2D接近：扩大合理滚动行程、平衡相机位移/旋转速度与停留段，连续受滚动控制，可中停和反向；不能只增加播放延时或引入独立世界时钟。
2. 每章2D背景对应的标题、正文、弱文本、链接、按钮、边框随表面主题协调变化；覆盖About/Life/Frame/Stack/Work/Contact及交接来源克隆，不能全局统一白字或黑字。
3. 移除开屏电脑内Index内容向下滑动的效果；保留真实电脑、Index内容与交互。移除初始灰幕上滑效果；定位真实来源，保留资源就绪/阅读fallback机制，不误删无关像素效果。
4. 修正全站2D返回3D：内容收回对应真实表面，再退出到房间；同一T的状态与双向连续性保持，书签独立保存。不能用覆盖整屏的假图/强制重置书签掩盖跳变。
5. 改善进入About的运动；另核对用户报告的左柜顶层摄像头与第三层明信片的空间关系和穿模。确认用户所指实体与运行时相机，不混淆两者。根据真实几何修正路径/目标/锚点/必要建模或动画，覆盖前后往返与近裁面。可修改Blender源及导出GLB，但先保存二进制备份，源与导出与契约同步，报告具体原因；无需再次等用户许可。切勿只移动碰撞检测盒或放宽容差。

## 所有权与实现范围

DEV负责以上需求相关的personal-archive、narrative语义/配置、阅读章节样式、入口/Loader具体效果及必要模型源、导出脚本/资产/契约。仅修改有因果关系文件，不恢复旧director执行链，不改无关工作。ARCH正独立只读调查HTML-in-Canvas，相关部署/Canvas修复留下一卡，不与其调查混入实现。你不是独占整个仓库，保留他人改动；PM只写卡片/看板。

开始记录分支/HEAD/工作区差异，核对NR-05接受61文件；每个将修改文件保存完整原文（GLB/Blend保存二进制），不得用HEAD替代当前基线。快照放output/pm/VR-01/，报告docs/pm/reports/VR-01-delivery.md。

## 技术验收与交付

- 先用简短中途报告说明真实根因、实现选择与所需模型变更，然后继续授权范围内实现，不停等PM形式确认。
- 必要纯采样/几何测试：真实GLB节点/动画及相机路径几何检查、端点和双向一致性、D1与阅读书签保留；普通配色不写镜像测试。
- 完整tsc-b、受影响lint、生产build及真正受影响守卫。旧守卫若强制本次明确删除效果，更新为新的行为约束并解释，不能删除保护换绿。
- 不运行Playwright、打开浏览器、截图或效果测试；这些由tim做。技术几何检查可运行headless Blender/Node，不当作视觉验收。
- 报告逐条需求实现映射、前后参数、准确检查结果、未验证边界、修改文件与完整原文/二进制候选。完成停止写入，通知PM。不得自行开启下一卡。
