# NR-Q03 · 连续照片与阅读返回独立验证

> 最新执行约定：沿用tim当前为负责会话配置的模型/推理设置，PM不覆盖；下方旧模型建议不再生效。额度中断后从已有成果续接。

**READY，NR-03已交付，PM已核验并冻结42文件候选。** QA / tim当前会话配置，产品只读。实际清单 `output/pm/NR-03/pm-candidate-files.json`；分支feat/narrative-kernel、HEAD55c08029051b11e9687d869747907e20291940fa。DEV已停止。PM独立静态检查可并行，产品不再修改。

先核候选代码/真实GLB/足球图指纹及DEV证据，按实际测试接口执行，不让本草稿假设限制实现。仅写 `docs/pm/reports/NR-Q03-photo-return-verification.md` 和 `output/pm/NR-Q03/`。不改产品/正式测试/DEV证据；需要复用测试时在自有输出目录保存仅更改证据路径的副本和变更说明。

独立验证三组完整结果：

1. 实际 Life→Frame 正反/中点直接进入及落墙：source/transfer/wall唯一可见，背纸同行；实际几何中心与边点、UV/足球身份一致，固定既有容差。浏览器证据结合真实GLB几何单测，不用同src、bbox或日志stamp替代。少量真实截图检查明显的双影、空白或穿帮，非美术验收。
2. 当前长文 About、Life、Frame深层主题返回物件再打开：收回的是当前阅读构图，快照inert、正文唯一可交互、原文/图片完整、书签恢复与有效焦点。新增请求或用户中断时旧相位不再提交/清除新状态。
3. 一次相位中resize及一次真实GPU恢复中换请求，恢复后照片/正文属于最新T。复验NR-Q02原暂态投影失败路径和必要永久绑定失败；失败正文可读，合法条件回来能够恢复，旧暂态缺陷不能复发。

优先复用已覆盖这些行为的DEV测试独立运行，补一条实际用户操作流程；不机械重跑全部旧套件。若一项确实未覆盖，报告缺口及最小补证，不能填推断PASS。使用原Playwright配置/系统Chrome/空闲非5173端口，保护用户服务。保留原容差与断言意义；少量诊断的有界观测不构成视觉认可。

交回PASS/FAIL/NOT_RUN、实际命令/退出码、证据和问题复现，源码前后指纹一致，清理自有服务后停止。技术缺陷交PM定点返工，不自行修改产品。视觉精修及最终收尾等待tim指示。

候选补充：DEV整组浏览器24通过/1超时，180秒预算的同用例及加强Frame最后簇另行2/2通过，不能改写成一次25/25。实际最终改动13文件，保护29文件；图片/GLB保持。深Frame截图部分使用style去掉外层投影以单看快照，请独立留一张未覆盖样式的真实返回相位画面。优先跑NR03四条+原Q02暂态恢复/GPU最新请求选定项，已有没受影响证据可复用，不重复全部25条。
