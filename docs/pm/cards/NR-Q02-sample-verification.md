# NR-Q02 · 样段候选独立技术验证

**READY，PM 已冻结 NR-02B 候选指纹**。QA / Sol high；读真实浏览器和代码，不改产品。相比机械检查，本卡涉及 WebGL 恢复、DOM 投影与路由中断，使用 Sol。

候选：同目录 `feat/narrative-kernel`，HEAD `55c08029051b11e9687d869747907e20291940fa` 叠加 `output/pm/NR-02B/pm-candidate-files.json`。PM 已独立核对实际文件与 DEV final-files 一致，并通过当前40单测/完整tsc/23文件lint。证据版本已更正：最先读取的20项浏览器通过属于上一轮，当前冻结版新增两项后的22项轮次在运行，`browser-final.json` 会更新，须以最终候选对应记录为准。DEV 仅补齐报告/证据，不改产品测试；QA 记录自己的独立命令与结果。

## 范围

仅验证 PM 指定的冻结候选版。先阅读 NR-02B 交付、NR-02P 的 V1–V7 和原 NR-Q01 映射；PM 同时审查代码，QA 负责实际运行与独立场景证据，不重复编写架构报告。

写入仅 `docs/pm/reports/NR-Q02-sample-verification.md` 与 `output/pm/NR-Q02/`，测试辅助程序也只能放该证据目录。不得修改产品、已有测试、配置或其他任务报告。执行前核对候选指纹；变动时停止该版本取证并告知 PM，不把不同版本的检查合并为 PASS。

## 必要检查

1. 运行候选版新增 execution 和原 shadow 浏览器目标用例；使用原 Playwright 配置、系统 Chrome 与空闲非 5173 端口。确认没有跳过、真实 WebGL 可用，故障夹具是真实触发目标路径。检查是否只对诊断字段断言而没读取实际场景/DOM。
2. 选至少一条与 DEV 用例不同的真实 UI 路径：Index → 长文中部 → 返回物件 → 再入原阅读位置 → 外章 → 回 About。核对正文及书签、物件 D1、唯一可交互正文和最终位置，不以 hash 变化代表成功。
3. 独立检查一次两次快速导航接续 resize，以及运行中的 GPU loss/restore 后新请求能否取代旧请求。分别保留实际位置/世界/最终相机/DOM与请求状态证据；若新测试已完整覆盖这条路径，可改用不同目标/进度复现，无需再建一套大型测试框架。
4. 渲染结果做功能观察：About 完整正文映射、Life/Frame 的真实照片、反向途中无残留第二份可点击正文，资源不可用时仍可读可返。保存能说明结果的少量截图；NR-02 的照片 endpoint-switch 是已批准临时基线，不按尚未实施 NR-03 的连续动效报错。

复用 DEV 已有完整类型/单元检查日志，不重跑无关全套。对潜在缺陷先给最小复现与预期/实际、稳定性、位置和证据；产品修复交 PM→DEV，QA 不直接改。自动通过不代替 tim 视觉认可。

## 交回

报告准确列出 checkout/候选指纹、命令及退出码、PASS/FAIL/BLOCKED/NOT_RUN、独立路径和未验证边界。只清理自己创建的临时预览与浏览器，不能动 tim 的 5173 服务。不得提交/推送/部署。完成后短报 PM 并停止。
