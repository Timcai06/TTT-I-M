# NR-Q05-R1 · 全站真实连续用户流程补证

**READY，2026-09-10 PM 派发；沿用 tim 当前 QA 模型，不改产品。**

NR-Q05 已独立通过分段采样和各专项，但报告“全站贯穿”把分段 T 取样与独立案例测试相加，尚不满足 NR-Q05 卡要求的一条真实连续用户流程。此为证据缺口，不认定产品 FAIL。保留原报告，补证单独报告。

候选仍为 `output/pm/NR-05/pm-candidate-files.json` 59文件。只写 `output/pm/NR-Q05-R1/` 与 `docs/pm/reports/NR-Q05-R1-real-user-journey.md`。复用现有环境、selectors 与隔离配置，不重复已过套件。

在一个正常启动且不中途 reload/reset 的真实桌面浏览器页面内：实际 Index 点击入口 → About 长文滚动 → Life → Frame 深主题/末簇 → Stack → Work 打开一个真实项目案例并关闭 → Contact 检查两个 CTA → 返回 About 或 Frame 并验证保存的阅读位置；通过可见按钮/导航链接与用户滚动驱动，不以直接调用 sample、滚动到诊断 T 或拼接独立页面替代。不点击对外发送/提交按钮。DOM 检查可以读取状态，但不得覆盖产品样式或伪造事件完成标志。

记录每一步动作、落点/正文可读与唯一交互/当前请求结果、案例焦点与滚动释放、返回书签，以及2至3张未改样式截图。如实际产品无法完成某一步，立即给出可复现缺陷；如无问题，准确 PASS。保存准确命令、退出码、原始结果；前后59指纹匹配、清理自有端口，保护5173。完成后停止。
