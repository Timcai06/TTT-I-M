# NR-00 · PM 最终规划验收

2026-09-09。状态：**ACCEPTED（规划交付）**。所有实施卡仍等待用户确认开工，不表示实现、运行或视觉通过。

## 复核结果

| 项目 | 判定 | 依据 |
| --- | --- | --- |
| R1 语义与具体绑定分离 | 通过 | v2 SemanticWorld 仅含样段需要的产品语义；11 动画及有效时间留在 SceneBinding；BindingReadback 独立，旧动作不可取时明确 unavailable |
| R2 首轮拆卡与观察隔离 | 通过 | A 纯采样、B 静态绑定与足球图身份、C 单点 shadow；Final Horizon 跨组件替换后移；A/B/C 都没有接管权 |
| D1 用户选择 | 已落实 | 物件由 T 推导，阅读书签用于选定目标 T；采样不消费访问历史或书签来改变同一 T 的世界 |
| 只读范围 | 通过 | PM 再查 67 条基线记录无差异；两份原报告 SHA 与修订回报一致；本轮仅规划文档变化 |
| 实施可执行性 AC-3 | 通过（规划） | 已核对现有 narrative 文件与测试组织；可形成明确的 NR-01A 文件白名单及独立验收，不需先运行全站 |

AC-1/2/4 延续前次 PM 静态审查，AC-3 本次完成。基线 HEAD `55c08029051b11e9687d869747907e20291940fa`；产品基线 `6137099`。本轮未编译草案、未运行测试、浏览器或服务。

## 进入任务卡的三条说明

1. `Readonly<StoryFrame>` 的类型标记本身不证明运行时无共享可变数据。A 要保证输入不被改动、先前输出不被后续采样改写；可以复制或冻结，不要求增加通用不可变状态框架。
2. A 只锁定语义端点与供后续使用的数值意图。具体相机空间路径、真实投影、曲面与资产解析都属于后续卡，不能为了填全接口提前接入 Three.js。
3. C 的 shadow 是诊断，不是另一个调度器。默认关闭、诊断失败不改旧 ready；生产渲染错误继续由原有错误路径处理，不能被诊断 catch 吞掉。

## 已收敛的产品与实施方向

- D1：tim 已确认按故事位置复原，保留阅读书签。
- D2：PM 选定保留现有足球图作为物件连续交接，随后展开原 Frame 文字内容；不新增图库条目，Final Horizon 独立。
- D3：PM 选定 A → B → C → NR-02 同帧接管 → NR-03 完整样段 → NR-04 全局协调 → 其余段落/精修/声音；即时 seek 只作中间交付。
- 资源体积继续是测量提示；已知 vendor 指纹问题单列，不伪称全套检查通过。

## 正式执行前状态

NR-00 已结束，不再要求 ARCH 扩写。首张 [NR-01A](../cards/NR-01A-semantic-sampling.md) 已准备为 DRAFT，由 DEV / Sol high 执行的建议不等于已派发。tim 确认这份计划并开工后，PM 才登记开发分支/文件所有权，切换 READY 并发送任务卡。

ARCH、DEV、QA 当前均无新的执行任务；本轮未创建会话、分支/worktree、提交或推送。
