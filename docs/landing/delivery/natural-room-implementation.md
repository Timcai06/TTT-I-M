# 本轮修订：光照配色与镜头对齐

2026-09-09 · 待 tim 视觉验收。

- 依据当前 [执行方向](../experience/natural-room-production.md) 完成统一阅读颜色：开屏期间从 WebGL 经输出色调映射后的书页外侧空白区域取样，中位数写入 `--room-paper`。全站阅读面、投影页、返回快照和底层伪元素共享颜色；不在滚动中重新取样，不产生逐帧颜色追逐。
- 清理了旧 About 纸色变黑、Frame 独立冷白、Stack 整章深色、Contact 独立白色与不一致透明边框。屏幕 Index、摄影、项目媒体仍保留自身图像色彩。
- 删除三组屏幕空间独立图片及对应位置/旋转关键帧。Frame → Stack 的照片属于电脑实体屏幕。
- 调整空间移动、法线/页面上方向对齐、物理靠近、最终视口接管的时序。最终接管只修正视口长宽比；不再把网页四角插值到相机前 0.03 米的平面。
- 退出使用实际章节的 DOM / Canvas / 视频静帧组成，替换原先近似的末屏模板。快照仅在开屏释放后捕获，不加入开屏资源等待，避免不可见副本阻塞加载。
- Contact 的阅读面固定在桌面世界坐标，采用模型纸材质；没有改动或新增 Blender 文件。
- 导航的收回、空间移动、展开依次执行，取消模糊图片与镜头运动重叠；保留滚轮中断、返回与阅读恢复。

验证：Landing 构建、类型检查、lint 通过；11 项投影/可逆进度/链接功能检查通过；六段双向共 85 帧像素抽查、1 次无效 HDR 注入及黑块检测负对照通过，运行错误 0；1200×760、1440×900 导航/中断/返回/Index 交互检查通过。报告：`output/playwright/archive-pixels.json`。未进行样式截图或美术验收；未提交、推送、部署。

取样在本次浏览器实际渲染输出上执行，不代表 Blender 与浏览器逐像素一致。取样结果在本次访问保持稳定；将来变更房间灯光时应重新加载以校准。

---
以下是前一阶段交付记录，其章节独立配色已由本轮取代。

# 自然光房间：本轮实现记录

2026-09-09 · 当前会话直接完成模型和前端接入。分支 main，起点 55c4ad3；既有 DEV v2 未提交改动完整保留。没有提交、推送或部署。美术验收由 tim 负责。

## 阅读入口
设计与工程约束见 [制作规范](../experience/natural-room-production.md)，参考站公开技术线索见 [Unseen 参考](../experience/unseen-technical-reference.md)。experience 管方向，assets 管资产规范，delivery 管实际实现与证据；旧交付不冒充现状。

## 模型
唯一工程：`/Users/tim/DEV/TTT I'M/portfolio/art/personal-archive/source/tim-cai-personal-archive.blend`。

大窗、亚麻帘、窗把手和台灯保留；第 17 集合新增独立山湖风景，后方和侧窗均有景观覆盖。原第 15 集合三维地形与森林隐藏保留，可恢复。玻璃原生菲涅耳透射仍保留，网页使用低透明度的玻璃材质，避免玻璃成为不透明面。视口预览降噪开启，128 samples。

素材：`art/personal-archive/textures/window/alpine-evening.png`，内置图像生成工具生成，1536×1024 原生尺寸，没有伪称 8K 或进行插值扩充。图像已放入项目并打包进 Blender。生成规格：独立阿尔卑斯山湖秋季夕照，柔和天空、层叠远山、森林湖岸与平静反光，无室内、窗框、家具、文字、噪点或装饰边框。图像为远景，室内近处窗框和物件提供真实视差；水面动态由网页局部 shader 处理。

源 SHA-256：4e04c95b48951a99975e94f9e9e326091e372106b6da77b9f1d4939be182e68b。保存后重新打开，图片依赖完整，书本/信件/照片/抽屉动画和阅读四角契约不变，没有新增 blend/blend1。

默认打开 WindowRoom_01_Overview，72 帧为打开的书本。若 Blender 正在打开旧版本，请先保存你自己的改动，再重新打开同一路径；磁盘更新不会替换已打开的内存场景。

## 网页
- 已重新烘焙 13 组原生材质，室内间接光图集提高到 4096，导出新的窗景房间与相机契约。
- 导出在内存移除隐藏旧墙体、旧风景与体积盒，保留源工程历史集合。玻璃与风景不参与静态房间合批，便于独立渲染。
- 79 个网页网格，模型 21.83 MiB。取消原 20 MiB 硬上限，不降低图片尺寸；其他数据契约仍检查。
- 入口摄影机使用新房间全景向显示器的轻微靠近；书本和屏幕近景使用新机位。保留既有 Index 的真实 HTML 交互、点击靠近、Escape 和滚动退出。
- 夕阳从背窗进入，调整天空补光、环境反射强度与台灯。开启已有 MSAA/FXAA 路径及有限值保护。
- 观察空间时镜头以阻尼微跟随鼠标，投影页使用同一最终相机；放大阅读和屏幕聚焦时跟随收敛，导航飞行不加鼠标扰动。
- 远景山体与树木图像静止，仅湖面中央使用低幅度 UV 扰动。隐藏标签停止持续绘制。
- About、Life、Work 与 Contact 改为共享暖纸面样式，调整文字、线条、头像阴影和阅读提示；投影与真实页面共用颜色和边距。Stack 与项目媒体保留合理的深色。
- 删除进入房间前的黑底淡入逻辑，常驻已预热房间；大幅降低遮罩，并关闭桌面空间中的全屏颗粒层。
- Frame 阅读载体改为偏冷的相纸白，照片和横向叙事保持；书本和抽屉热点随模型投影定位。
- Frame → Stack 保留现有最后照片与屏幕同图交接，不回到独立 Stack 标题卡。
- 直接跳章的克隆页携带原页面主题变量与章节标记，防止失去原有 ID 后变回黑底。

## 检查与范围
模型契约检查、Landing 类型检查、生产构建通过。像素功能抽查 85 帧（六段双向各七点与 Index），非有限值故障注入一次，另有半屏黑块负例，错误列表为空。这不是全帧证明或美术验收。

1200×760 与 1440×900 下的跳章中断、窗口调整、Index 聚焦/退出及渲染器所有权检查通过。Landing lint 通过。没有截图比对、样式自动验收、移动端测试或 Studio 修改。不把技术通过描述成达到获奖级别；最终画面、节奏与沉浸感由 tim 验收。
