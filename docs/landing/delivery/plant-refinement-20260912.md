# 绿植模型优化 · 2026-09-12

在原始 `art/personal-archive/source/tim-cai-personal-archive.blend` 内完成。保留花盆锚点 `(-1.46, 1.11, 2.08)`，只替换和新增 `Plant ` 前缀的网格，材质使用独立 `Plant /` 命名。

## 改动

- 盆体：96 段旋转截面，盆口、内壁、圆润边沿与下凹土面；陶质微纹理及少量土壤颗粒。
- 结构：6 根上生枝、4 根垂蔓，40 片叶片沿枝条生长；垂蔓在越过实际层板边缘后下降。
- 叶片：每片 231 个基础顶点，非对称心形轮廓、拱起和卷曲，0.35 mm 厚度。
- 材质：局部 UV 叶脉、色素变化、细微凹凸、适度的粗糙度与少量透光。

## 复现和过程

`tools/personal_space/modeling/refine_plant.py` 的 `stage_pot()`、`stage_growth()`、`stage_surface()` 分阶段在 Blender 内执行。脚本不会自行保存或导出。

本地 `output/plant-refinement/process.html` 包含实际视口截图和前后滑动对照。`source-before.bin` 是修改前源文件的精确副本，用于恢复，并非另一个工作模型。

## 检查与边界

- 比对前后 1936 个非绿植物体的网格坐标、拓扑、面材质索引、平滑标记、变换、父级、渲染隐藏标记和材质绑定名称：一致。范围未包括全部材质节点参数和修改器参数。
- 脚本语法解析通过；源文件原地保存。
- 当前仅更新 Blender 源模型，未导出网页 GLB、重新烘焙或部署。过程截图采用 Blender 材质预览环境，不构成前端视觉验收。
- 未来接入网页时需将程序化叶脉、颜色和粗糙度烘焙为 glTF 兼容贴图；沿用现有灯光/阴影导出约定，避免覆盖已接受的书本和印刷品阴影结果。

后续网页资产接入记录见 [三轮模型优化接入网站](refined-room-web-20260912.md)。本页保留源模型轮次的检查范围。
