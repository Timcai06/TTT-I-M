# 电脑屏幕显示修正

日期：2026-09-07。针对 tim 指出的默认项目图被替换、竖幅风景在横向屏幕中缺乏真实应用界面的反馈，仅调整 Blender 屏幕显示层。

- 默认恢复 `projects/pulsegraph/live-monitor.webp` 项目画面。
- 照片状态使用已有 `frame/scenery/scenery-05.webp` 原图，1206 × 1305，保留原始比例。此前使用的是 720 宽版本。
- 增加独立可编辑的窗口工具栏、窗口控件、缩略图栏、选中背景、文件名、Fit 提示、缩放符号和底部尺寸信息；照片置于留白工作区中。
- 所有显示内容仍在原显示器边框内，没有修改显示器、桌面或房间几何。状态切换使用 Blender 对象可见性关键帧，不能视为已经可直接导出到网页的交互实现。

## 查看

唯一工程继续使用 `art/personal-archive/source/tim-cai-personal-archive.blend`。

- 第 1–129 帧：项目画面。
- 第 130–159 帧：照片查看器。
- 第 160 帧起：恢复项目画面。
- 屏幕特写相机：`Review_11_MonitorClose`。

预览目录：`art/personal-archive/reviews/monitor/`，包含 `01-project-default.png` 和 `02-photo-viewer.png`。清单为 `manifest.json`，记录这一版的模型哈希；此前其他预览目录里的哈希只代表相应历史版本。

本轮检查图片分辨率、宽高比、状态可见性及网站资源哈希，生成两张预览交由 tim 检查，不进行视觉评分。没有新增 blend 文件，也没有修改前端或网页 GLB。

## 支架遮挡修复

tim 指出支架挡住显示区域后，读取模型确认立柱前沿在屏幕前方，顶部与显示区域重叠。已把原立柱移到屏幕后方，将底座移到其下方，并增加连接背壳的支架连接块；屏幕内容和两种显示状态保持原样。

从房间、工作站和显示器特写三个相机向屏幕采样点做了 225 次支架射线相交检查，无支架遮挡。只验证几何关系，不作视觉验收。最新工程哈希见 `reviews/monitor/stand-fix.json`，两张屏幕预览已按修复后的工程重新生成；原 `manifest.json` 保留为屏幕界面修正时的记录。
