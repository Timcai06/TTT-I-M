# 个人空间模型验收版

日期：2026-09-07。状态：Blender 制作及静帧交付，等待 tim 判断效果。当前不接入网站，不修改手机端或 Studio。

## 本轮执行边界

按 tim 最新指示，先完成模型，再由 tim 看效果；收到后续明确指示后，才更新网页模型并按整站方案开发。此前文档中的“局部建模后立即导出”不适用于当前阶段。

唯一活跃工程仍是 `art/personal-archive/source/tim-cai-personal-archive.blend`。没有新增 `.blend` 或 `.blend1`；既有历史文件未清理。网页使用的 `personal-space.glb` 和 `cameras.json` 与本轮开始时 SHA-256 一致。它们暂时与源工程不同，这是有意的阶段隔离。

## 实际制作

| 部位 | 本轮变化 |
| --- | --- |
| About | 保留开书、独立阅读表面和页边细节；增加书脊折痕及细小页脚标记。书页仍是网页正文将来进入的物理表面 |
| Life | 信封改为独立三角翻盖；照片换成项目中已有的足球照片，添加纸质底托及阅读锚点，保留照片抽出动作 |
| Frame | 四张真实照片保持比例；图像与厚纸底分层，共用弯曲轮廓；添加挂环和连接钩、独立纸张枢轴及轻微摆动动作 |
| Stack | 显示器加入已有 PulseGraph 截图、屏幕锚点、背壳、散热槽、接口及电源线；键盘改为深色键帽，添加字符、空格键、修饰键和鼠标滚轮 |
| Work | 下方四个浅抽屉保留，上方重组为一个深抽屉；增加内壁、导轨、把手、标签、六份独立文件夹、首份档案封面与真实项目图；抽屉和首份档案独立动画 |
| 房间 | 增加窗边花器、枝条和三脚凳；背墙与窗外使用项目已有照片；错开地板木纹采样位置，调整表面法线强度及木材涂层 |
| 灯光与镜头 | 调整窗光、台灯和补光；保存七个命名相机，使用 Cycles、1600 × 1000、64 samples 与降噪输出供 tim 查看 |

## 打开与查看

预览和清单位于 `art/personal-archive/reviews/current/`。

| 图片 | Blender 相机 | 帧 | 内容 |
| --- | --- | --- | --- |
| `01-room.png` | `Review_01_Room` | 1 | 房间静止全景 |
| `02-about.png` | `Review_02_About` | 25 | 笔记本打开 |
| `03-life.png` | `Review_03_Life` | 25 | 信封、照片、杯子近景 |
| `04-frame.png` | `Review_04_Frame` | 1 | 照片墙和挂件 |
| `05-stack.png` | `Review_05_Stack` | 1 | 显示器和桌面工具 |
| `06-work.png` | `Review_06_Work` | 110 | 抽屉拉开、首份档案提起 |
| `07-room-open.png` | `Review_07_RoomOpen` | 110 | 物件展开后的全景 |

重新打开活跃工程即可看到磁盘上的修改，已打开的 Blender 内存场景不会自动刷新。重新打开前处理自己的未保存修改，避免随后把旧内存状态覆盖回磁盘。

默认相机为全景。在 Blender 的 `08 Model review cameras` 集合选中目标相机，在 3D 视图使用 `Ctrl + 小键盘 0` 设为活动相机；按表格切换时间轴帧数。没有小键盘时，可通过 3D 视图的 View / Cameras 菜单设置活动对象为相机。已有帧标记：1 静止、25 书和 Life 展开、72 抽屉展开、110 文件提起。相机的 `review_frame` 自定义属性也记录了对应帧。

## 技术记录与待验收

本轮清单为 `model-manifest.json`；记录模型 SHA-256、对象和网格数量、动作、表面节点、四个状态的位置、已打包图片及未变更的网站资源哈希。渲染日志为 `renders-all.json`。

完成了结构、有限坐标、关键动作位移、图片打包和输出文件完整性检查。未做视觉检验，也未把离线渲染视为浏览器效果证明。Blender 灯光和渲染器与网页运行时不同，网页画质、动作衔接和性能要在 tim 指示接入后单独实现。

tim 可按整体氛围、物件造型、材质光影和近景构图给出修改意见。视觉通过不自动等于网站接入授权；网站接入以 tim 后续明确指示为准。
