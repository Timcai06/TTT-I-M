# Public Chrome Origin Trial token bound to https://www.crt-dsg.com.
# Configure this only in Vercel Production; local development should keep using
# runtime capability detection or the canvas-draw-element browser flag.
HTML_IN_CANVAS_ORIGIN_TRIAL_TOKEN=
